- name: Push VRRP configuration commands to IOS XR
  hosts: ios-xr
  gather_facts: false
  connection: local

  tasks:
    - name: configuration commands are running
      cisco.iosxr.iosxr_config:
        lines:
        - name master
        - priority 130
        - address 192.168.23.2
        parents:
        - router vrrp 
        - interface GigabitEthernet0/0/0/1
        - address-family ipv4
        - vrrp 130

      register: print_output

    -  debug: var=print_output.stdout_lines

    - name: save output to a file
      lineinfile:
        create: yes
        line: "{{item}}"
        path: ./output/{{ inventory_hostname }}.txt
      with_items: "{{ print_output.stdout }}"

# Commands to run:
#1) ansible-playbook ios-xr_conf_vrrp_aach.yaml -u akahric -k
# !!Please change to your username and use your radius password when prompted
